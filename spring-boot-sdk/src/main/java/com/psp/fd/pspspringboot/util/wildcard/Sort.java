package com.psp.fd.pspspringboot.util.wildcard;

public class Sort<T extends Comparable<T>> {
//public class Sort<T extends Number & Comparable<T>> { //multiple bounds for generic param -> must provide two bounds

}
//public class Util <T extends Number> generic bounding to make sure that it only accept Number type
//Number type {Integer, Double, Float,...}
